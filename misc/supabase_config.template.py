# PCT GeoGuesser — Supabase configuration template
#
# Copy this file to misc/supabase_config.py and fill in your project's values.
# Find them in: Supabase dashboard → Project Settings → API
#
# The anon key is the "publishable" key — safe to embed in client HTML.
# Never use or commit the service-role ("secret") key.

SUPABASE_URL      = "https://your-project-ref.supabase.co"
SUPABASE_ANON_KEY = "your-anon-public-key"
